import {useParam} from 'react-dom'


const Pokemon = () => {
    
    return(
        <div>
            Pokemon
            {/* <div>
                {data && data.filter(e => id ? id === e.id : e)
                .map(e => e.name.english)}
            </div> */}
        </div>
    )
}

export default Pokemon;